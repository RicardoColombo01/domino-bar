{
  "name": "domino-bar",
  "version": "1.7.0",
  "private": true,
  "description": "Dominó de bar em 3D: 2 a 4 jogadores, bots opcionais e online P2P sem servidor",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check",
    "test": "node build.mjs && cd tests && npm test",
    "telas": "node build.mjs && cd tests && npm run telas",
    "lembrar": "node build.mjs && cd tests && npm run lembrar",
    "shots": "node build.mjs && cd tests && npm run shots",
    "online": "node build.mjs && cd tests && npm run online",
    "fechamento": "node build.mjs && cd tests && npm run fechamento",
    "servir": "npx --yes serve ."
  }
}
